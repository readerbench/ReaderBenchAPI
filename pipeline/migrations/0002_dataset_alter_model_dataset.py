# Generated by Django 4.2.3 on 2023-08-29 11:59

from django.conf import settings
from django.db import migrations, models
import django.db.models.deletion


class Migration(migrations.Migration):

    dependencies = [
        ('services', '0005_job_dataset'),
        migrations.swappable_dependency(settings.AUTH_USER_MODEL),
        ('pipeline', '0001_initial'),
    ]

    operations = [
        migrations.CreateModel(
            name='Dataset',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('name', models.CharField(max_length=50)),
                ('task', models.CharField(max_length=50)),
                ('num_rows', models.IntegerField(default=0)),
                ('num_cols', models.IntegerField(default=0)),
                ('lang', models.ForeignKey(on_delete=django.db.models.deletion.CASCADE, to='services.language')),
                ('user', models.ForeignKey(on_delete=django.db.models.deletion.CASCADE, to=settings.AUTH_USER_MODEL)),
            ],
        ),
        migrations.RunSQL("""
            INSERT INTO pipeline_dataset (
                id,
                name,
                task,
                num_rows,
                num_cols,
                lang_id,
                user_id
            )
            SELECT
                id,
                name,
                task,
                num_rows,
                num_cols,
                lang_id,
                user_id
            FROM
                services_dataset;
        """, reverse_sql="""
            INSERT INTO services_dataset (
                id,
                name,
                task,
                num_rows,
                num_cols,
                lang_id,
                user_id
            )
            SELECT
                id,
                name,
                task,
                num_rows,
                num_cols,
                lang_id,
                user_id
            FROM
                pipeline_dataset;
        """),
        migrations.AlterField(
            model_name='model',
            name='dataset',
            field=models.ForeignKey(on_delete=django.db.models.deletion.CASCADE, to='pipeline.dataset'),
        ),
    ]
